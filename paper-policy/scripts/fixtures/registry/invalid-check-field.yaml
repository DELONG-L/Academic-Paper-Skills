version: 1
rules:
  - id: TEST.HARD_RULE
    title: Invalid hard check
    force: hard
    scope: [workflow]
    artifacts: [policy]
    phases: [draft]
    activation: {type: profile, when: [test-profile]}
    requirement: Reject malformed flow mappings.
    checks:
      - kind: deterministic
        evidence_required: false
        description: Truncated description.
        unintended fragment: null
    failure: {draft: block, final: block}
    autofix: none
    waiver: {allowed: false, authorities: []}
    decision_refs: [A01]
    source: {origin: local-house-policy, note: Test fixture.}
